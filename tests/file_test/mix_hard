while [ a<b ]
do
    case $a in
        1) case $a in
            1) echo "a"
            2) case $a in
                1) case $a in
                    1) echo "a"
                    2) for a in `seq 1 10`;
                        do
                            for b in `seq 1 10`;
                            do
                                for c in `seq 1 10`;
                                do
                                    echo a
                                done
                            done
                            for z in `seq 1 $b`;
                            do
                                echo a
                            done
                        done
                    *) echo "lol"
                esac
                2) case $a in
                    1) echo "a"
                    2) echo "b"
                    *) echo "lol"
                esac
                *) case $a in
                    1) echo "a"
                    2) while [ a==z ]
                        do
                            echo "mel"
                        done
                    *) echo "lol"
                esac
            esac
            *) echo "lol"
        esac
        2) case $a in
            1) for a in `seq 1 10`;
                do
                    for b in `seq 1 10`;
                    do
                        for c in `seq 1 10`;
                        do
                            echo a
                        done
                    done
                    for z in `seq 1 $b`;
                    do
                        echo a
                    done
                done
            2) echo "b"
            *) echo "lol"
        esac
        *) case $a in
            1) echo "a"
            2) echo "b"
            *) echo "lol"
        esac
    esac
    while [ a<b ]
    do
        while [ a==f ]
        do
            while [ a<b ]
            do
            if [ a==b ]
            then
                if [z]
                then
                    echo "try"
                fi
                if [ a!=w ]
                then
                    if [i]
                    then
                        while [ a!=l ]
                        do
                            echo "mal"
                        done
                        if [ a!=o ]
                        then
                            if [t]
                            then
                                echo "try"
                            fi
                        fi
                    fi
                fi
            done
        done
    done
done