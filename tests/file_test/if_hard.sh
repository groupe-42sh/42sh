if [ a==b ]
then
    if [z]
    then
        echo "try"
    fi
    if [ a!=w ]
    then
        if [i]
        then
            if [ a!=o ]
            then
                if [t]
                then
                    echo "try"
                fi
            fi
        fi
    fi
fi