case $a in
    1) case $a in
        1) echo "a"
        2) echo "b"
        *) echo "lol"
    esac
    2) case $a in
        1) echo "a"
        2) echo "b"
        *) echo "lol"
    esac
    *) case $a in
        1) echo "a"
        2) echo "b"
        *) echo "lol"
    esac
esac